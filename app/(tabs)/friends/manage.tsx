// Friends management (code, requests, find, friend list). Reached from the Feed tab's
// top-right people button. Renders the shared screen in app/(tabs)/profile/friends.tsx.
export { default } from '../profile/friends';
